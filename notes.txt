-----for creating database in terminal------

from app import app, db

with app.app_context():
    db.create_all()



---to create product inside-----

.\env\Scripts\Activate.ps1
python



from app import app, db, product   # or Product if you renamed the model

with app.app_context():
    # now you are inside the application context
    p1 = product(name="Blue Formal Shirt", price=599, image="images/formal1.jpeg", category="shirts")
    p2 = product(name="Black pants", price=799, image="images/pant1.jpeg", category="pant")
    db.session.add_all([p1, p2])
    db.session.commit()